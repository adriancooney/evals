# Skill Installation

<prompt>
Create a new temp directory and cd into it. Run `npx skills add adriancooney/evals -y` to install the evals skill.
</prompt>

<expectation>
The skill installs successfully. The output shows "Found 1 skill" and "Installation complete" with the evals skill installed to at least one agent.
</expectation>
